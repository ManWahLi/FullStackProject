<p style="color: green"><%= notice %></p>

<%= render @category %>

<div class="container mt-3">
  <%= link_to "Edit this category", edit_category_path(@category), class: "btn btn-outline-primary" %>
  <%= link_to "Back to categories", categories_path, class: "btn btn-outline-primary" %>

  <%= button_to "Destroy this category", @category, method: :delete, class: "btn btn-outline-danger mt-3" %>
</div>
