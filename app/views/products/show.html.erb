<p style="color: green"><%= notice %></p>

<%= render @product %>

<div class="container mt-3">
  <%= link_to "Edit this product", edit_product_path(@product), class: "btn btn-outline-primary" %>
  <%= link_to "Back to products", products_path, class: "btn btn-outline-primary" %>

  <%= button_to "Destroy this product", @product, method: :delete, class: "btn btn-outline-danger mt-3" %>
</div>
